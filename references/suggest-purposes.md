1. `srp suggest-purposes --output json` — generates rule-based candidates and auto-stages them.
2. Enhance: produce improved candidates as JSON matching `{"purpose_candidates":[{"name":"...","method":"...","reason":"..."}]}`.
3. `echo '<json>' | srp stage-suggestions --from-stdin`
