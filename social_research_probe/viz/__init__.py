"""Visualisation package that renders social-media metric data as chart images.

Charts are saved to a temporary directory as PNG files. Each module returns
the file path and a human-readable caption so the caller can embed or display them.
"""
